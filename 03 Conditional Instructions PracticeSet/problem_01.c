#include<stdio.h>

int main()
{
    int a = 10;
    if (a = 11)     // = : assign value , == this is for checking equals and if ke andar ek non zero value hai so true so first if will run 
        printf("I am 11");
    else    
        printf("I am not 11");
    return 0;
}
