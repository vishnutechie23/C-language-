#include<stdio.h>

int main(){
    int age = 15;
    if (age > 20){
        printf("we are inside if - 1\n");
        printf("Your age is greter than 10"); // if condtion is true will work
    }
    else 
        printf("Your age is less than 20"); // if condition is false 
    return 0;
}